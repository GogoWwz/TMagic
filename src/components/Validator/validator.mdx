---
name: Validator
route: /Form/Validator
menu: Form
edit: false
---

import { Playground } from 'docz'

import Validator from './index'
import Input from '../Input'
import { Num } from './rules'

<Playground>
  <Validator validates={{
    num: {
      rule: Num,
      message: "请输入纯数字:自定义提示"
    }
  }}>
    {/* <Input></Input> */}
  </Validator>
</Playground>