---
name: Validator
route: /Form/Validator
menu: Form
edit: false
---

import { Playground } from 'docz'

import Validator from './index'
import Input from '../Input'

<Playground>
  <Validator validates={{
    num: {
      message: "请输入纯数字"
    },
    min: {
      value: 5,
      message: "最小长度为5"
    }
  }}>
    <Input></Input>
  </Validator>
</Playground>