---
name: Message
route: /Feedback/Message
menu: Feedback
---

import { Playground } from 'docz'
import Button from '../Button'
import Message from './message'

<Playground>
  <Button type="primary" onClick={() => Message.info()}>Show Info</Button>
  <Button type="success" onClick={() => Message.success()}>Show Success</Button>
  <Button type="warning" onClick={() => Message.warning()}>Show Warning</Button>
  <Button type="danger" onClick={() => Message.danger()}>Show Danger</Button>
</Playground>